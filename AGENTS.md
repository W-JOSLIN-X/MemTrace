# MemTrace 仓库 Agent 执行规则

## 1. 适用范围与优先级

本文件适用于整个仓库。任何 Agent、协作者或后续对话在修改代码、契约、测试、文档、Git 分支或 GitHub 设置前，都必须完整阅读本文件。

发生冲突时按以下顺序处理：

1. 用户在当前对话中的明确要求；
2. 本文件；
3. `docs/OWNER_LED_COLLABORATION_WORKFLOW.md`；
4. 可执行契约、实际代码、数据库迁移与本轮实际测试结果；
5. `docs/MEMTRACE_D2_D7_TWO_PERSON_EXECUTION_PLAN.md` 与总计划；
6. 旧 PR、旧 Prompt、旧 HANDOFF、旧核验报告及其中记录的历史流程。

旧文档中的“必须开 PR”“必须由另一名协作者审批”“禁止所有人直接 push main”“通过 integration/day2 两级晋级”等要求，自 2026-08-24 起均为历史流程，不再约束 Day 4–Day 7。若旧报告与当前代码或测试不一致，以当前用户要求和本轮实际证据为准。

## 2. 固定角色与权限边界

- 仓库所有者、每日集成者和最终发布者：`W-JOSLIN-X`（成员 B）。
- 协作者：`zlbk-wxy`（成员 A）。
- 成员 A 只在自己当天的功能分支开发、commit 和 push；绝不直接 push、force-push、merge 或改写 `main`。
- 成员 B 从远端取得成员 A 的分支，独立审查、测试和修复，在其基础上完成成员 B 当日任务，并在完整门禁通过后以普通非强制 push 直接更新 `main`。
- 日常交付不要求 PR，也不要求成员 A 审批成员 B 的最终提交。PR 可以在用户另行要求、平台必须或需要公开评审时使用，但不能被 Agent自行重新设为完成前提。
- 成员 B 直接更新 `main` 的权限不授权跳过测试、不授权 force push，也不授权忽略远端并发更新。

## 3. 每日标准流程

### 3.1 成员 A 开发阶段

1. 从执行开始时的最新 `origin/main` 创建 `feat/a-dN-*` 分支。
2. 只完成当天 Prompt 指定的成员 A 范围；共享契约变化必须同时更新所有规范投影和测试。
3. 运行与改动相称的测试，检查秘密、正文日志、数据库和临时产物。
4. 使用普通 commit 保存工作并 push 到该功能分支；不 rebase 已经交接的提交，不 squash、amend 或重写已经推送的历史。
5. 向成员 B 交接：分支名、完整 base/head SHA、提交列表、实际命令/退出码/测试数量、契约与迁移变化、已知失败、明确未实现项和所需登录。
6. 交接后等待；除非成员 B 明确要求，不继续向该分支追加提交。

### 3.2 成员 B 接管与整合阶段

1. 先 `git fetch --prune origin`，核对 `origin/main`、协作者分支和交接 SHA；若 head 变化，审查新增差异后再继续。
2. 从精确的最新 `origin/main` 创建 `codex/dayN-owner-integration`，用普通 `--no-ff` merge 引入成员 A 分支，保留作者和提交祖先；不得通过 rebase、squash、amend 改写协作者历史。
3. 独立阅读代码、契约、迁移和测试，不把交接报告中的“通过”当作本轮证据。
4. 先运行成员 A 相关门禁并复现问题；修复阻断后，再在同一整合分支完成成员 B 当日任务。
5. 运行从 G1 到当天 Gx 的完整递增门禁，包括后端、前端、契约、迁移、Docker、浏览器、持久化、隔离、隐私和秘密检查中与当天范围相符的项目。
6. 记录最后已知可用 commit、实际命令、退出码、数量、容器/浏览器证据、限制和降级状态。

### 3.3 所有者直接交付 `main`

只有成员 B 可以执行。推送前必须：

1. 再次 fetch，并确认将要推送的 `origin/main` 与整合时已经审查和测试的 base 一致。
2. 若远端 `main` 已移动，停止推送；先把新远端提交普通 merge 到整合分支，重新审查冲突并重跑受影响门禁。
3. 确认协作者交接 head 是当前整合 head 的祖先，工作区没有意外 tracked/untracked 产物，`git diff --check` 通过。
4. 使用普通 `git push origin HEAD:main`；永远不得使用 `--force`、`--force-with-lease`、删除分支保护或管理员绕过来掩盖非快进/测试问题。
5. 推送后读取远端 `main`，确认其 SHA 等于本地已验证 head，并再次检查工作区。

只有远端 `main` 已更新且上述证据成立，才能报告“Day N 完成”。若代码和测试已就绪但 GitHub 登录、网络或远端状态阻塞，只能报告具体阻塞，不能报告完成。

## 4. 契约、数据与产品边界

- 共享 Schema/API/Event/数据库语义变更必须先写 change note，再同步 Pydantic、JSON Schema、实际 OpenAPI、TypeScript 类型与 runtime parser、fixtures 和测试。
- 无需再开独立 contract PR；成员 A 提交契约草案，成员 B 在本地整合时做最终核验和冻结。
- UI 不得把 pending/candidate/retrieved/injected 写成 active/applied/learned；产品状态以持久化后端状态为准。
- 任务类别由服务端自动识别，用户不得提交或选择 `scenario`。
- owner 隔离、幂等、事务、持久事件、恢复和 metadata-only 日志是跨日不可回退的门禁。
- 不把后续天数能力提前伪装为已完成；降级必须明确、可观察且有测试。

## 5. 登录、秘密、隐私与安全

- 开始使用需要账号的工具前，先说明需要登录什么。执行中首次发现真实登录失效或新增登录要求时，立即暂停并告诉用户；不得绕过登录去找非登录替代路径。
- 若沙箱/代理阻断与登录失效表现相似，先用获准的同一官方工具做最小复核；确认真实登录失效后立即停下。
- 默认硬门禁使用 `MOCK_MODE=true`；除非用户明确要求，不读取、不索取真实 Provider Key，也不把真实模型 smoke 作为 Day 4 的完成门禁。
- `.env`、token、SQLite 数据库、用户正文、编辑稿、证据正文、模型缓存和浏览器资料不得提交 Git，也不得出现在 URL、日志、截图或交接消息中。
- `owner_id` 只能来自验证后的 session。跨用户资源统一遵循契约规定的不可见语义。
- 不运行未授权的任意代码、Shell、文件系统或网络工具；不扩大当天明确范围。

## 6. Git 安全与现有工作保护

- 编辑前检查 `git status`；用户已有改动不可覆盖、丢弃或归为自己的提交。
- 不使用 `git reset --hard`、`git checkout --`、递归删除或其他破坏性命令处理未知工作。
- 不删除协作者远端分支，除非用户另行明确要求。
- 每个 commit 保持单一可解释目标，使用 `feat`、`fix`、`test`、`docs`、`chore` 前缀。
- `integration/day2`、PR #1–#6 和相关分支保留为 Day 2/Day 3 历史证据；Day 4 起不再作为发布必经分支。

## 7. 严谨报告要求

- 有问题直接指出，不用旧交接数字替代实际结果，不把“测试文件存在”写成“功能可用”。
- 明确区分：已从当前代码验证、仅由文档声明、尚未测试、因外部条件阻塞。
- 测试结果必须包含命令、退出码和实际数量；浏览器/容器结果必须说明环境和恢复边界。
- 未完成项、P1/P2、真实 Provider 未验证项和任何降级必须显式列出。
