import { PlaceholderPage } from '../components/PlaceholderPage'

export function SettingsPage() {
  return (
    <PlaceholderPage
      day="D1+"
      description="这里将承载 Provider 模式、记忆开关和安全设置；真实模型密钥始终只保存在服务端环境变量中。"
      eyebrow="Settings"
      title="设置功能将在后续步骤接入"
    />
  )
}
